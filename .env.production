# Produção — usado por `npm run build` / `npm run build:deploy`
# e por `npm run build:release` (PDV; sobrescreve a API do .env.desktop).
VITE_API_URL=https://gaspassapp.com.br
RELEASES_URL=https://gaspassapp.com.br/releases/
